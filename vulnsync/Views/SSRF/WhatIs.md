## What is SSRF?
**SSRF (Server-Side Request Forgery)** is a security vulnerability where an attacker tricks a server into making unintended requests. This can expose internal services, bypass firewalls, or leak sensitive data. It occurs when user-controlled input is used to fetch URLs without proper validation

---

**Account login details :**
**UserName:** user  
**Password:** user123
