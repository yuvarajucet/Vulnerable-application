## Server Side Template IInjection (SSTI)
SSTI (Server-Side Template Injection) is a web security vulnerability that occurs when user input is improperly handled in server-side templates. Templates are used to dynamically render web pages, and when malicious input is injected into these templates, it can lead to the execution of arbitrary server-side code.
